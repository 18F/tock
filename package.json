{
  "private": true,
  "scripts": {
    "build-css": "node-sass --source-map true --include-path ./node_modules/uswds/src/stylesheets tock/tock/static/sass -o tock/tock/static/css/dist",
    "watch-css": "npm run build-css -- --watch",
    "prestart": "CYPRESS_INSTALL_BINARY=0 npm install --quiet",
    "start": "npm run build-css && npm run watch-css",
    "predeploy": "CYPRESS_INSTALL_BINARY=0 npm install --silent && npm run build-css",
    "cypress:open": "cypress open"
  },
  "engines": {
    "node": ">=8.10.0"
  },
  "dependencies": {
    "chosen-js": "^1.8.7",
    "jquery": "^3.3.1",
    "node-sass": "^4.9.1",
    "uswds": "^1.6.9"
  },
  "devDependencies": {
    "cypress": "^3.1.2"
  }
}
